fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# Embeddings:
#   Option A (auto-detected): Azure OpenAI text-embedding-3-small
#   Option B (fallback):      Pinecone built-in multilingual-e5-large
# openai package needed for Option A — harmless to install even if not used
openai==1.51.0

# Vector DB — Pinecone
pinecone==5.4.2

# Web search fallback (Tavily — optional, set TAVILY_API_KEY to enable)
httpx==0.27.2

# Document parsers
PyMuPDF==1.24.11        # PDF
python-docx==1.1.2      # Word
openpyxl==3.1.5         # Excel
pandas==2.2.3           # Excel/CSV tabular data
beautifulsoup4==4.12.3  # URL scraping
requests==2.32.3        # URL fetch

# LangChain for chunking only
langchain-text-splitters==0.3.0
